apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-{{ .Values.component}}-pg-config
data:
  POSTGRES_DB: {{ .Values.configmap.database | quote}}
  POSTGRES_USER: {{ .Values.configmap.user | quote}}
  POSTGRES_PASSWORD: {{ .Values.configmap.password | quote }}
