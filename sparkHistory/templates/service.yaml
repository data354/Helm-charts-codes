apiVersion: v1
kind: Service
metadata:
  name: {{ include "sparkHistory.fullname" . }}
  labels:
    {{- include "sparkHistory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sparkHistory.selectorLabels" . | nindent 4 }}
