apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sparkhistory.fullname" . }}-test-connection"
  labels:
    {{- include "sparkhistory.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sparkhistory.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
