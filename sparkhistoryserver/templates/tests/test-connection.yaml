apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sparkhistoryserver.fullname" . }}-test-connection"
  labels:
    {{- include "sparkhistoryserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sparkhistoryserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
