apiVersion: v1
kind: Service
metadata:
  name: {{ include "sparkhistoryserver.fullname" . }}
  labels:
    {{- include "sparkhistoryserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sparkhistoryserver.selectorLabels" . | nindent 4 }}
